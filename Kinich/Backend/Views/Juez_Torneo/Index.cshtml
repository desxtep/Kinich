@model IEnumerable<Domain.Juez_Torneo>

@{
    ViewBag.Title = "Index";
}

    <h2>Juez_Torneo</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Torneo.Nombre)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Usuario.NombreUsuario)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Area)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Autorizado)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Torneo.Nombre)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Usuario.NombreUsuario)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Area)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Autorizado)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id_user_juez }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id_user_juez }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id_user_juez })
        </td>
    </tr>
}

</table>
